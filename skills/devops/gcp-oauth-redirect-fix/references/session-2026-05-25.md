# Session 2026-05-25 — Google OAuth + MindCoach Calendar Tab

## Task
Configure Google OAuth for robertosantos.una@gmail.com, sync to neural network, add Calendar tab to MindCoach app.

## Key Discoveries

### OAuth Setup
- **redirect_uri_mismatch**: client_secret.json was outdated. GCP Console had different redirect URIs than the JSON. Fix: manually add `http://localhost:1` in Console.
- **access_denied**: OAuth consent screen in Testing mode, email not in test users. Fix: add to audience page.
- **APIs disabled**: Calendar API not enabled — `--check-live` failed. Fix: `gcloud services enable calendar-json.googleapis.com`.
- **ALWAYS use setup.py**: The google-workspace skill's `scripts/setup.py` handles PKCE, state, code_verifier. Never build custom OAuth.
- **CDP redirect capture**: Faster than user paste — grab URL from `http://localhost:9222/json/list` after authorization.

### MindCoach App
- **Chat bridge**: Removed auto "Recebido!" reply from mindcoach_bridge.py. Added `chat_to_app.py` script for agent to send real responses via WebSocket.
- **Dock bar bugs**: togglePanel() was empty, X close buttons didn't remove dock-up consistently, calendar panel used style.display instead of classList.open.
- **Calendar backend**: Added `/calendar` endpoint to chat_api.py using Google Calendar API v3. Token sourced from file or GOOGLE_TOKEN_B64 env var (base64 for Cloud Run).
- **Deploy pattern**: Cloud Run + env var for token.

### Anti-patterns confirmed
- AP-15: Starting OAuth without checking skills wasted ~30 turns. setup.py existed the whole time.
- Never automate GCP Console UI (Angular SPA).
- Never use ydotool blind Tab navigation on unknown pages.
